import type { Transformer } from "./index.js";

export const createTransformInjectStyles = (styles: Record<string, string>) => {
	const transformInjectStyles: Transformer = async ({ content, filePath }) => {
	
    };

    return transformInjectStyles;
};